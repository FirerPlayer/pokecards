
function getData(query) {
    const baseUrl = "https://pokeapi.co/api/v2/"
    fetch("")
}

class Pokemon {
    constructor(name = "") {
        if (name != "") {
            this.name = name
        } else {
            this.page = getPage()
        }
    }

    getPage
}